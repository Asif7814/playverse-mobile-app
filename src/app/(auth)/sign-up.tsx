import { Text, View } from "react-native";
import { styles, textStyles } from "@/src/theme/styles";

export default function SignUpScreen() {
    return (
        <View style={styles.container}>
            <Text style={textStyles.xl}>Sign Up</Text>
        </View>
    );
}
